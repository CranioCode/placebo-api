import express from "express";

const router = express.Router();

// router.post("/new", newMedicine);
// router.get("/:id", getMedicineById);
// router.get("/search", getMedicineByName);

export default router;
